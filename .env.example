# Required for npm run dev (login button)
# After terraform apply, this is the lambda_auth_function_url output + "auth/login"
PUBLIC_LOGIN_URL=https://<id>.lambda-url.us-east-1.on.aws/auth/login

# Required for npm run sync
GITHUB_TOKEN=ghp_...
GITHUB_ORG=navapbc
